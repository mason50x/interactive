@AGENTS.md

# Typography

Never use uppercase or letter-spacing styling. That means no `uppercase`
(or `text-transform`) and no `tracking-*` / `letter-spacing` anywhere —
not on labels, eyebrows, badges, headings, or display type. Type is set at
its natural case and its natural spacing.
